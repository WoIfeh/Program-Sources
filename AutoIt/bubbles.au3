

// Local Variables
Local coord1, coord2, coord3, coord4;


// Principle based on bubble sort algorithm
If coord1 > coord 2 Then
  _swap(coord1, coord2);
EndIf

If coord3 > coord4 Then
  _swap(coord3, coord4);
EndIf

If coord2 > coord4 Then
  _swap(coord2, coord4);
EndIf

smallestCoord = coord1;
largestCoord = coord4;

